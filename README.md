# Graphics Playground

A playground for different graphics APIs.

## Cloning

I use some submodules of some header libraries, so pull them down with the clone:

```sh
git clone --recurse-submodules git@github.com:pgalasti/graphics-stuff.git
```

If you already cloned without them:

```sh
git submodule update --init --recursive
```

See [opengl/README.md](opengl/README.md) for the libraries you need and how to build.
